//Collect the Users Date 
let userDate = document.getElementById("userDate");

// Add an event listener to capture the date when it changes
userDate.addEventListener("change", function() {
    console.log(userDate.value);
});

